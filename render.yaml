services:
  - type: web
    name: academy-web
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: academy_db  
          property: connectionString
      - key: SECRET_KEY
        generateValue: true  
